<h2>Settings</h2>

<br><br>

<%= link_to image_tag("stripe.png"), "https://connect.stripe.com/oauth/authorize?response_type=code&client_id=#{STRIPE_CLIENT_ID}&scope=read_write&state=1234" %><br>
<%= "Your stripe account is set up!" if current_user.stripe_user_id != nil && current_user.stripe_user_id != "" %>